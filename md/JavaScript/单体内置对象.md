
# 单体内置对象


## Global对象

isNaN()、isFinite()、parseInt()以及parseFloat()，实际上全都Global对象的方法。

- URI编码方法:encodeURI()和encodeURIComponent();
    - encodeURI: 不会对本身属于URI 的特殊字符进行编码，例如冒号、正斜杠、问号和井字号；除了空格之外的其他字符都原封不动，只有空格被替换成了%20
    - 和encodeURIComponent: 会对它发现的任何非标准字符进行编码
- URI解码方法：decodeURI()和decodeURIComponent()。
    - decodeURI：只能对使用encodeURI()替换的字符进行解码
    - 和decodeURIComponent： 能够解码使用encodeURIComponent()编码的所有字符，即它可以解码任何特殊字符的编码








